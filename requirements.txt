openpyxl>=3.1.0
xlrd>=2.0.1                   # чтение старого бинарного .xls (parsers/xlsx_parser)
python-docx>=1.1.0
pdfplumber>=0.11.0
PyMuPDF>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0

# --- Нормализатор (normalizer/) ---
# Все три опциональны: модуль работает и без них (stdlib-фолбэки),
# но с ними точность выше. См. docs/NORMALIZER_SPEC.md §1-3.
rapidfuzz>=3.0.0              # лексический fuzzy (ступень 4)
pymorphy3>=2.0.0             # лемматизация русского (§1)
sentence-transformers>=2.2.0 # семантические эмбеддинги LaBSE/MiniLM (§3)
numpy>=1.24.0                # косинусная близость эмбеддингов
# Доступ к БД (repository.py / db.py)
psycopg[binary,pool]>=3.1.0
python-dotenv>=1.0.0         # чтение DATABASE_URL из .env (db.py)

# --- REST API + веб-консоль оператора (main.py, этап 4) ---
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9      # приём ZIP-архива через POST /upload
